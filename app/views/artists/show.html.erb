<div class="show">

	<h1 style ="text-align:center"><%= @artist["name"] %></h1>

	<div class="image"><img src="<%= @artist["image"][4]["content"] %>"/></div>
	<% if @artist["bio"]["content"] == {} %>
		Sorry No Bio for this Artist!
	<% else %>	
		<%= @artist["bio"]["content"].html_safe %>
	<% end %>
	<div class="clearfix"></div>

</div>